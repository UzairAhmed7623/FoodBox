<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodbox">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/food_box_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/food_box_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodBox">
        <activity android:name=".TrackOrders"></activity>
        <activity android:name=".OrderHistory" />
        <activity android:name=".RestaurantItems" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>